# Frontend

React application for the bank system.

## Setup

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Build for production: `npm run build`

## Features

- User authentication
- Account management
- P2P transfers
- Admin dashboard
